VITE_API_URL=http://localhost:5001

# Set by frontend/.env.demo for `npm run build:demo` (static build with sample data, no
# backend needed — see README "Demo mode"). Leave unset for the normal dev/build.
# VITE_DEMO_MODE=true
